// Generated by CoffeeScript 1.6.2
(function() {
  define(["ural/modules/pubSub", "app/vm/curUser"], function(pubSub, curUser) {
    var ActiveGraph, Menu;

    ActiveGraph = (function() {
      function ActiveGraph() {
        this.id = ko.observable();
        this.name = ko.observable();
        this.isYours = ko.observable();
      }

      ActiveGraph.prototype.save = function(data, event) {
        event.preventDefault();
        return pubSub.pub("graph", "save");
      };

      return ActiveGraph;

    })();
    return Menu = (function() {
      function Menu() {
        var _this = this;

        this.active = ko.observable();
        this.activeGraph = new ActiveGraph();
        pubSub.sub("href", "changed", function(data) {
          return _this.active("/" + data.controller + "/" + data.action);
        });
        this.user = new curUser();
      }

      Menu.prototype.load = function(filter, done) {
        var _this = this;

        return this.user.load(filter, function(err) {
          return done(err, _this);
        });
      };

      return Menu;

    })();
  });

}).call(this);

/*
//@ sourceMappingURL=menu.map
*/
