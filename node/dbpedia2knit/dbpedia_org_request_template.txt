
    {0}
    select {1} ?s, ?name, ?label, ?foaf_name
	where
	{
        		{2}
                optional { ?s dbpprop:name ?name }
                optional { ?s rdfs:label ?label }
                optional { ?s foaf:name ?foaf_name }
	}
